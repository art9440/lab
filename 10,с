#include <stdio.h>
#include <malloc.h>


int symm(int *arr, int i, int j){
    if (arr[i] != arr[j])
        return 0;
    if ((i + 1 == j + 1) || (i + 1 == j && j - 1 == i))
        return 1;

    return symm(arr, i + 1, j - 1);

}

int main(){
    int len, *arr, i, j;
    scanf("%d", &len);
    arr = (int*)malloc(len * sizeof(int));

    for (int k = 0; k < len; k++)
        scanf("%d", arr + k);
    scanf("%d %d", &i, &j);

    if (symm(arr, i - 1, j - 1) == 1)
        printf("Symmetrical");
    else
        printf("Not Symmetrical");

    free(arr);
    return 0;
}
